{
  "description": "Anwaltsgeheimnis (attorney-client privilege) detection for Swiss law practice. Scans Write/Edit/MultiEdit/WebFetch and all MCP tool inputs (mcp__.*) before content leaves the machine.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|WebFetch|mcp__.*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/privacy-check.js",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
