{
  "name": "@bettercallclaude/entscheidsuche",
  "version": "2.0.0",
  "description": "MCP server for Swiss federal court decision search (Entscheidsuche)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "dev": "npx ts-node src/index.ts",
    "test": "vitest run",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "mcp",
    "swiss-law",
    "entscheidsuche",
    "court-decisions"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@bettercallclaude/shared": "file:../shared"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.7.2"
  }
}
