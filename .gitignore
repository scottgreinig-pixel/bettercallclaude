# Dependencies
node_modules/

# TypeScript build intermediates
*.tsbuildinfo

# MCP server source build outputs (intermediate — NOT the checked-in compiled bundles)
mcp-servers-src/*/dist/
mcp-servers-src/*/node_modules/
mcp-servers-src/*/coverage/
mcp-servers-src/*/.env

# NOTE: bettercallclaude/mcp-servers/*/dist/ is NOT ignored — compiled bundles
# are checked into git so end users don't need a build step

# Runtime data (SQLite databases created at runtime)
bettercallclaude/mcp-servers/*/data/

# Package outputs
dist/mcpb/
dist/mcpb-staging/
dist/*.zip

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Environment and secrets
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
tmp/
temp/
*.tmp

# Project instructions (local only)
CLAUDE.md

# Python (for development tooling)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# Test coverage
coverage/

# Development artifacts
claudedocs/
.serena/

# Generated installation metadata
manifest.json
version.txt
AGENTS.md
